# =============================================================================
# dragnet-gpu — FastAPI Gateway Dependencies
# Redis and Celery removed — RunPod handles queuing internally.
# =============================================================================

fastapi==0.115.12
uvicorn[standard]==0.34.2
httpx==0.28.1
python-dotenv==1.1.0
pydantic==2.11.5
redis==5.2.1
fastembed>=0.3.0